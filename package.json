{
  "dependencies": {
    "connect-mongo": "^5.0.0",
    "eris": "^0.17.2",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "mongodb": "^5.8.0",
    "node-fetch": "^3.3.1"
  },
  "name": "grotto-bestiary",
  "main": "index.mjs",
  "scripts": {
    "dev": "node -r dotenv/config index.mjs dotenv_config_path=.env.local",
    "start": "node index.mjs",
    "heroku-postbuild": "cd client && npm ci && npm run build"
  },
  "devDependencies": {
    "dotenv": "^16.3.1"
  }
}
